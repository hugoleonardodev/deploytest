import { Pagination } from 'reactstrap'
import styled from 'styled-components'

export const PatientPagination = styled(Pagination)`
    .pagination.pagination-lg {
        justify-content: center;
    }
`
